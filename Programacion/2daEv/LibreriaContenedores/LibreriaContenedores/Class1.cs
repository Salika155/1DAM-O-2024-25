namespace LibreriaContenedores
{
    public class Class1
    {
        // record -> parecido al constructor, public en este caso, y se le pueden pasar los elementos directamente como si fuesen properties
        // cons -> valores constantes para usar -> static readonly
        // struct -> new (en este caso no es crear un objeto, invoca al constructor de objeto)
            //Vector V = new Vector(); -> variables de stack. -> variables como int y otros tipos de datos.
                                    // -> variables de heap. -> cuando creas variables nuevas de objeto -> otro sitio de la memoria reservado. (interface, clase, etc)


        /* struct Vector
         * {
         * double x;
         * double y;
         * }
         * 
         * {
         * int a = 0;
         * Vector v = new Vector();
         * }
            
         */

        
    }
}
